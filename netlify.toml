# Netlify configuration file
[build]
  # Publish directory (root contains all static files)
  publish = "."
  # Install dependencies for Netlify Functions (only if package.json exists)
  command = "npm install || true"

# Functions directory
[functions]
  directory = "netlify/functions"

# Redirect rules for better URL handling
[[redirects]]
  from = "/shop"
  to = "/pages/shop.html"
  status = 301

[[redirects]]
  from = "/shop/"
  to = "/pages/shop.html"
  status = 301

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
